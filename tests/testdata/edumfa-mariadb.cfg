# Keep the MariaDB CI environment aligned with the documented production
# configuration. The audit tests also use a separate SQLite database, so its
# engine must not inherit the MariaDB-specific isolation level.
SQLALCHEMY_ENGINE_OPTIONS = {"isolation_level": "READ COMMITTED"}
EDUMFA_AUDIT_SQL_OPTIONS = {}

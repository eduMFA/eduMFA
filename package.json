{
  "dependencies": {
    "grunt-angular-gettext": "2.5.3",
    "grunt-cli": "1.5.0",
    "grunt-init": "0.3.2"
  }, 
  "scripts": {  
    "translate": "grunt nggettext_extract && for language in po/*.po; do msgmerge -U --backup=off $language po/template.pot; done && grunt nggettext_compile"
  }
}

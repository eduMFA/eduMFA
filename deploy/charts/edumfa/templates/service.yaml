apiVersion: v1
kind: Service
metadata:
  name: {{ include "edumfa.fullname" . }}
  labels:
    {{- include "edumfa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "edumfa.selectorLabels" . | nindent 4 }}

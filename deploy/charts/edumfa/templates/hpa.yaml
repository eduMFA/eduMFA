{{- if .Values.edumfa.worker.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "edumfa.fullname" . }}
  labels:
    {{- include "edumfa.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "edumfa.fullname" . }}
  minReplicas: {{ .Values.edumfa.worker.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.edumfa.worker.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.edumfa.worker.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.edumfa.worker.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.edumfa.worker.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.edumfa.worker.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
